# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
.env
.venv
env/
venv/

# Tests / caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Build artifacts
build/
dist/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/

######### SPECIFIC

# Generated by the app at startup / on each regeneration. Rebuilt by
# `task fetch`, the Docker image, or the scheduler, so they are never tracked.
static/index.html
static/index.html.br
static/index.html.gz
static/data/
static/channels/
static/**/*.br
static/**/*.gz
static/**/*.tmp
